# Antigravity adapter

Antigravity agents must load `.agents/rules/repository-policy.md`, which delegates to the canonical root `AGENTS.md` policy and its task-specific `.agent/` documents.

Use the shared FreePi prompts under `.agent/review-prompts/` for mandatory independent review gates. Keep personal configuration under ignored local paths; do not commit it.
